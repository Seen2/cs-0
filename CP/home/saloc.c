#include <unistd.h>
#include <stdio.h>
#include <signal.h>
#include <stdlib.h>
#include <string.h>
int main(void)
{
int n;
printf("\n enter the no of items\n");
scanf("%d",&n);
char* w=malloc(n*sizeof(char));
printf("\n enter the string\n");
int pause(void);
int m=n;
fgets(w,m,stdin);
puts(w);
}
